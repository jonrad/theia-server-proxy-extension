{
  "private": true,
  "scripts": {
    "preinstall": "node-gyp install",
    "bootstrap": "lerna bootstrap",
    "prepare": "lerna run prepare",
    "rebuild:browser": "theia rebuild --target=browser",
    "start:browser": "yarn rebuild:browser && yarn --cwd browser-app start --hostname 0.0.0.0",
    "watch": "lerna run --parallel watch",
    "build:docker": "docker build . -t jonrad/theia-datascience",
    "publish:docker": "yarn build:docker && docker push jonrad/theia-datascience"
  },
  "devDependencies": {
    "lerna": "2.4.0"
  },
  "resolutions": {
    "**/**/nsfw": "1.2.9"
  },
  "workspaces": [
    "theia-server-proxy-iframe-extension",
    "theia-server-proxy-extension",
    "theia-server-proxy-list-extension",
    "theia-jupyter-extension",
    "theia-rstudio-extension",
    "browser-app"
  ],
  "homepage": "https://github.com/jonrad/theia-server-proxy-extension",
  "version": "0.0.1"
}
